-- inserts to bmb outside of creat script
-- Filename for homework: sql instruction/bmdb/adv-maintenance.sql

-- 1 add 5 new movies, but don't define ID
-- 2 add a new actor(s), but don't define ID
-- 3 add related credit entries

-- Optional, classic-model - full
